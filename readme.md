To run API:
1 - choose version of node to run using nvm
nvm use "node version"

2 - use npm to start the local host
npm run start

3 - open up the localhost either manually in your web browser or via the command line

